#include "lists.h"
#include <stdlib.h>
#include <stddef.h>
#include <stdio.h>

/**
 * is_palindrome -  a function in C that checks
 * if a singly linked list is a palindrome
 * @head: head pointer
 *
 * Return: 1 if it is a palindrome or 0 ortherwise
 */
